import React from 'react';
import AppLayout from '/components/AppLayout/AppLayout'

export default class extends React.Component {
    render(){
        return(
            <AppLayout>
                <h1>Labs.</h1>
                <p>
                    Ampersand is divided in different sections, and each organ has a specific function. We created the labs in order for outside ideas to “come to life”, even if the creator of it doesn’t have the resources to make it for themselves. 
                </p>
            </AppLayout>
        )
    }
}